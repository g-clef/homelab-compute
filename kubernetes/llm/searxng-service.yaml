# kubernetes/llm/searxng-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: llm
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: searxng
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP